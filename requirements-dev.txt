# Development & test dependencies
pytest
pytest-asyncio
pytest-cov
requests
redis
fastapi
jsonschema
openai
pydantic
types-requests

# Formatters / linters (for dev installs only; pre-commit installs their pinned versions)
ruff
black==25.11.0
isort==5.13.2

# Multi-tenant support (required for tests)
bcrypt
sqlalchemy
asyncpg
